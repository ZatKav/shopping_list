<%= form_for(@list) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :name, placeholder: "Give your list a name" %>
  </div>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>

