<% provide(:title, 'New item') %>
<h1>New Item</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@item) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Create my item", class: "btn btn-large btn-primary" %>

    <% end %>
  </div>
</div>
